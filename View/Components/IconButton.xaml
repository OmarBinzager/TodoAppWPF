<UserControl x:Class="ToDoProject.View.Components.IconButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoProject.View.Components"
             mc:Ignorable="d" Width="34" Height="34">
        <UserControl.Template>
            <ControlTemplate>
                <Border Width="34" Height="34" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}" CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <Canvas>
                        <Path Margin="10" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}" Data="{Binding Data, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </Canvas>
                </Border>
            </ControlTemplate>
        </UserControl.Template>
</UserControl>
